# 🚀 Полный гайд по деплою на Railway

## Что такое Railway?

Railway.app - это платформа для хостинга приложений, которая предоставляет:
- ✅ **Бесплатный план** - $5 в месяц бесплатных кредитов
- ✅ **24/7 работа** - ваш бот будет работать круглосуточно
- ✅ **Автодеплой** - автоматическое обновление при push в GitHub
- ✅ **Простая настройка** - деплой за 5 минут
- ✅ **Логи в реальном времени** - мониторинг работы бота

## 📋 Предварительная подготовка

### 1. Проверьте готовность проекта

```bash
# Убедитесь, что все файлы на месте
ls Procfile railway.json nixpacks.toml runtime.txt requirements.txt

# Проверьте, что .env НЕ в git репозитории
git status
# Если видите .env - срочно добавьте его в .gitignore!
```

### 2. Подготовьте GitHub репозиторий

```bash
# Создайте репозиторий на GitHub (если ещё не создан)
# https://github.com/new

# Инициализируйте git (если ещё не инициализирован)
git init
git add .
git commit -m "Initial commit - готов к Railway деплою"

# Добавьте remote и запушьте
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

### 3. Получите данные для переменных окружения

Вам понадобятся:
- `BOT_TOKEN` - токен вашего Telegram бота (от @BotFather)
- `ADMIN_CHAT_ID` - ваш Telegram Chat ID (получите у @userinfobot)

## 🚂 Деплой на Railway (пошагово)

### Шаг 1: Регистрация на Railway

1. Перейдите на https://railway.app
2. Нажмите **"Login"** или **"Start a New Project"**
3. Войдите через GitHub (рекомендуется)
4. Подтвердите доступ к репозиториям

### Шаг 2: Создание проекта

1. На главной странице нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий **RPRZbOT-main** (или как вы его назвали)
4. Railway автоматически определит Python проект

### Шаг 3: Настройка переменных окружения

1. После создания проекта откройте вкладку **"Variables"**
2. Добавьте переменные (кнопка **"+ New Variable"**):

```env
BOT_TOKEN=ваш_токен_от_botfather
ADMIN_CHAT_ID=ваш_chat_id
LOG_LEVEL=INFO
MAX_FILE_SIZE_MB=20
MAX_VIDEO_SIZE_MB=300
SPAM_LIMIT=5
```

**ВАЖНО:**
- BOT_TOKEN должен быть в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
- ADMIN_CHAT_ID должен быть числом, например: `123456789`

### Шаг 4: Настройка деплоя

1. Откройте вкладку **"Settings"**
2. В разделе **"Deploy"** убедитесь:
   - ✅ **Watch Paths** не установлены (будет следить за всем репозиторием)
   - ✅ **Auto Deploy** включен
   - ✅ **Branch** установлен на `main` (или ваша основная ветка)

3. В разделе **"Environment"**:
   - Python Version: `3.10.12` (или как в runtime.txt)

### Шаг 5: Первый деплой

1. Railway автоматически начнет деплой
2. Следите за логами во вкладке **"Deployments"**
3. Дождитесь сообщения: `Бот подключен: @ваш_бот_username`

### Шаг 6: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Убедитесь, что бот отвечает

## 📊 Мониторинг и логи

### Просмотр логов в реальном времени

1. Откройте проект в Railway
2. Перейдите на вкладку **"Deployments"**
3. Кликните на активный деплой
4. Нажмите **"View Logs"**

### Типы логов

- **Build Logs** - логи установки зависимостей
- **Deploy Logs** - логи запуска бота
- **Runtime Logs** - логи работы бота в реальном времени

### Health Check

Railway будет автоматически проверять работу бота через:
- HTTP endpoint: `/health`
- Проверка каждые 5 минут

## 🔄 Обновление бота

### Автоматическое обновление (рекомендуется)

```bash
# Внесите изменения в код
# Закоммитьте и запушьте
git add .
git commit -m "Обновление функционала"
git push origin main

# Railway автоматически задеплоит новую версию!
```

### Ручной редеплой

1. Откройте проект в Railway
2. Перейдите на вкладку **"Deployments"**
3. Нажмите **"Redeploy"** на последнем деплое

## ❌ Устранение проблем

### Бот не запускается

**Проблема:** Deploy failed или бот не отвечает

**Решение:**
1. Проверьте логи в Railway (вкладка "Deployments")
2. Убедитесь, что `BOT_TOKEN` правильный:
   ```bash
   # Проверьте формат токена
   # Правильно: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   # Неправильно: токен без ":" или короткий
   ```
3. Проверьте `requirements.txt` - все ли зависимости установлены

### Конфликт экземпляров (409 Error)

**Проблема:** `A request to the Telegram API was unsuccessful. Error code: 409`

**Решение:**
1. Остановите локальную версию бота (если запущена)
2. В Railway: Settings → Service → Restart
3. Подождите 30 секунд

### Превышен лимит Railway

**Проблема:** Бот остановился, Railway показывает "Out of credits"

**Решение:**
1. Проверьте использование ресурсов в Railway Dashboard
2. Бесплатный план: $5/месяц (≈ 500 часов работы)
3. Если превышен лимит - добавьте кредитную карту или подождите начала месяца

### Проблемы с зависимостями

**Проблема:** `ModuleNotFoundError` в логах

**Решение:**
1. Проверьте `requirements.txt` - все ли библиотеки указаны
2. Убедитесь, что версии Python совпадают (локально и в Railway)
3. Редеплой: Deployments → Redeploy

## 💰 Стоимость и лимиты

### Бесплатный план Railway

- ✅ **$5 бесплатных кредитов в месяц**
- ✅ **Примерно 500 часов работы** (при минимальном потреблении)
- ✅ **1000 часов с картой** ($0 списываний, но карта нужна для подтверждения)
- ❌ Нет автоматического масштабирования

### Оптимизация расходов

1. **Отключите неиспользуемые сервисы**
2. **Используйте легкие образы** (Python 3.10 вместо 3.12)
3. **Рабочие часы** (опция в коде - закомментирована):
   ```python
   # В bot/main.py есть функция is_working_hours()
   # Можно включить для работы только с 7:00 до 19:00 МСК
   ```

## 🔐 Безопасность

### Переменные окружения

✅ **НИКОГДА НЕ КОММИТЬТЕ:**
- `.env` файлы
- Токены ботов
- API ключи
- Пароли

✅ **ВСЕГДА ИСПОЛЬЗУЙТЕ:**
- Railway Variables для секретов
- `.gitignore` для исключения `.env`

### Проверка безопасности

```bash
# Убедитесь, что .env не в репозитории
git log --all --full-history -- .env

# Если находит - удалите из истории:
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all
```

## 📱 Мониторинг в Telegram

Бот автоматически отправляет уведомления админу:
- ❗ Критические ошибки
- 🚨 Инциденты безопасности
- 💡 Новые предложения
- 📊 Ежедневная статистика (опционально)

Для настройки Email уведомлений:
```env
YANDEX_SMTP_ENABLED=true
YANDEX_SMTP_HOST=smtp.yandex.ru
YANDEX_SMTP_PORT=587
YANDEX_SMTP_USER=your_email@yandex.com
YANDEX_SMTP_PASSWORD=your_app_password
```

## 🎉 Готово!

Ваш бот теперь работает 24/7 на Railway!

### Следующие шаги:

1. ✅ Протестируйте все функции в Telegram
2. ✅ Настройте уведомления (если нужно)
3. ✅ Поделитесь ботом с командой
4. ✅ Мониторьте логи первые дни

### Полезные ссылки:

- 🚂 Railway Dashboard: https://railway.app/dashboard
- 📚 Railway Docs: https://docs.railway.app
- 💬 Railway Discord: https://discord.gg/railway
- 🤖 Telegram Bot API: https://core.telegram.org/bots/api

---

**Нужна помощь?** Создайте issue в репозитории или обратитесь в Railway Support.

**Удачного деплоя!** 🚀
