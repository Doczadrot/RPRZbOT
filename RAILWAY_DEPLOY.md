# 🚀 Инструкция по деплою на Railway

## 📋 Настройка переменных окружения

### Обязательные переменные:

1. **BOT_TOKEN** - токен вашего Telegram бота
   - Получите у @BotFather в Telegram
   - Формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

2. **ADMIN_CHAT_ID** (опционально) - ID чата для уведомлений об опасности
   - Получите через @userinfobot в Telegram

### Опциональные переменные:

3. **WEBHOOK_URL** - НЕ ОБЯЗАТЕЛЬНО!
   - Код автоматически определяет URL из `RAILWAY_PUBLIC_DOMAIN`
   - Если хотите указать вручную, формат: `https://your-app.railway.app/webhook`

4. **PORT** - НЕ НУЖНО указывать
   - Railway автоматически устанавливает эту переменную

## 🔍 Как узнать URL вашего приложения в Railway

### Способ 1: Через Railway Dashboard (автоматически)

1. Откройте ваш проект в Railway Dashboard
2. Перейдите в настройки сервиса (Settings)
3. Найдите раздел "Networking" или "Public Domain"
4. Railway автоматически создаст домен вида: `your-app-name.up.railway.app`
5. **Код автоматически использует этот домен!** Ничего добавлять не нужно.

### Способ 2: После первого деплоя

1. После успешного деплоя откройте вкладку "Settings" вашего сервиса
2. В разделе "Networking" будет указан Public Domain
3. URL будет вида: `https://striking-alignment-production.up.railway.app`
4. Webhook URL будет: `https://striking-alignment-production.up.railway.app/webhook`

## ⚙️ Где добавить переменные окружения в Railway

1. **Откройте Railway Dashboard**
2. **Выберите ваш проект** (striking-alignment)
3. **Выберите сервис** (worker)
4. **Перейдите во вкладку "Variables"**
5. **Нажмите "+ New Variable"**
6. **Добавьте переменные:**
   - `BOT_TOKEN` = ваш токен бота
   - `ADMIN_CHAT_ID` = ID админ-чата (опционально)

**ВАЖНО:** `WEBHOOK_URL` добавлять НЕ НУЖНО! Код автоматически использует `RAILWAY_PUBLIC_DOMAIN`.

## ✅ Проверка работы

После деплоя:

1. **Проверьте логи** в Railway Dashboard → вкладка "Logs"
   - Должно быть: `✅ Webhook установлен: https://...`
   - Должно быть: `⏰ Время запуска: ...`

2. **Проверьте health endpoint:**
   - Откройте: `https://your-app.railway.app/health`
   - Должен вернуть JSON с информацией о времени работы

3. **Проверьте главную страницу:**
   - Откройте: `https://your-app.railway.app/`
   - Должно показать: "Bot Running" и время работы

4. **Протестируйте бота в Telegram:**
   - Найдите вашего бота
   - Отправьте `/start`
   - Бот должен ответить

## 🔧 Если что-то не работает

### Проблема: "WEBHOOK_URL не найден"

**Решение:** Это нормально для первого запуска. Railway автоматически создаст домен после деплоя. Просто подождите несколько минут и перезапустите деплой.

### Проблема: "Deployment failed"

**Решение:**
1. Проверьте, что в Settings сервиса включен "Serverless" режим
2. Проверьте логи на ошибки
3. Убедитесь, что `BOT_TOKEN` установлен правильно

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи - должен быть `✅ Webhook установлен`
2. Проверьте, что webhook URL доступен: откройте в браузере `https://your-app.railway.app/webhook`
3. Проверьте токен бота в переменных окружения

## 📝 Итого

**Минимальная настройка:**
- ✅ Добавить только `BOT_TOKEN` в Variables
- ✅ `WEBHOOK_URL` добавлять НЕ НУЖНО (определяется автоматически)
- ✅ `PORT` добавлять НЕ НУЖНО (устанавливается Railway)

**После деплоя бот автоматически:**
- ✅ Определит свой URL из Railway
- ✅ Установит webhook в Telegram
- ✅ Начнет логировать время работы

