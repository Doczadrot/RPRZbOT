# 🚂 Деплой на Railway

## Настройка для бесплатного плана (Serverless)

### 1. Подготовка проекта

Проект уже настроен для деплоя на Railway:
- `railway.json` - конфигурация Railway
- `Procfile` - команда запуска
- `nixpacks.toml` - конфигурация сборки

### 2. Создание проекта на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Выберите репозиторий `RPRZ_DANGER`

### 3. Настройка переменных окружения

В настройках сервиса добавьте переменные:

```
BOT_TOKEN=ваш_токен_бота
DISABLE_WORKING_HOURS=1
ADMIN_CHAT_ID=ваш_chat_id
```

**Важно:** Установите `DISABLE_WORKING_HOURS=1` для работы 24/7 на Railway, так как проверка времени может конфликтовать с часовыми поясами сервера.

### 4. Включение Serverless режима

**КРИТИЧЕСКИ ВАЖНО для бесплатного плана:**

1. Перейдите в настройки сервиса (Settings)
2. Найдите опцию "Serverless"
3. **Включите флаг "Serverless"**
4. Сохраните изменения

Без этого флага деплой на бесплатном плане не будет работать!

### 5. Деплой

После настройки Railway автоматически:
- Соберет проект
- Установит зависимости
- Запустит бота

### 6. Проверка работы

1. Проверьте логи в разделе "Deploy Logs"
2. Убедитесь, что бот запустился без ошибок
3. Протестируйте бота в Telegram

## Рабочие часы

Бот настроен на работу с **07:00 до 19:00 МСК** каждый день.

Для отключения проверки времени установите:
```
DISABLE_WORKING_HOURS=1
```

## Мониторинг

- Логи доступны в разделе "Logs" на Railway
- Метрики в разделе "Observability"
- История деплоев в разделе "History"

